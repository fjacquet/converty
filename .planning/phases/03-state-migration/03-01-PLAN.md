---
phase: 03-state-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-converter.ts
  - src/hooks/use-url-state.ts
autonomous: true

must_haves:
  truths:
    - "Developer searches codebase for useConverter and finds zero imports"
    - "Files use-converter.ts and use-url-state.ts do not exist in src/hooks/"
    - "TypeScript compilation passes with zero errors after deletion"
  artifacts:
    - path: "src/hooks/use-converter.ts"
      status: "DELETED"
      provides: "Legacy state management hook (no longer used)"
    - path: "src/hooks/use-url-state.ts"
      status: "DELETED"
      provides: "Legacy URL sync hook (replaced by middleware)"
  key_links:
    - from: "All calculator components"
      to: "src/stores/calculator-store.ts"
      via: "createCalculatorStore import"
      pattern: "import.*createCalculatorStore"
    - from: "No calculator components"
      to: "src/hooks/use-converter.ts"
      via: "N/A (zero imports expected)"
      pattern: "from.*use-converter|from.*use-url-state"
---

<objective>
Remove legacy state management hooks after verifying migration is complete.

Purpose: Complete the state management consolidation by removing unused legacy code (useConverter and useUrlState hooks). The migration to Zustand stores is already complete - all 117 calculators use createCalculatorStore. This plan safely removes the now-obsolete hooks and verifies the codebase remains functional.

Output: Legacy hook files deleted, TypeScript compilation passing, codebase ready for manual testing verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-state-migration/03-RESEARCH.md
@.planning/phases/02-url-sync-infrastructure/02-01-SUMMARY.md

@src/stores/calculator-store.ts
@src/hooks/use-converter.ts
@src/hooks/use-url-state.ts
</context>

<tasks>

<task type="auto">
  <name>Verify migration complete - zero legacy imports</name>
  <files>N/A (verification only)</files>
  <action>
    Run grep searches to verify zero imports of legacy hooks in calculator files:

    1. Search for useConverter imports:
       ```bash
       grep -r "from.*use-converter" src/app --include="*.tsx"
       ```
       Expected: No matches (exit code 1)

    2. Search for useUrlState imports:
       ```bash
       grep -r "from.*use-url-state" src/app --include="*.tsx"
       ```
       Expected: No matches (exit code 1)

    3. Count calculators using Zustand:
       ```bash
       grep -r "createCalculatorStore" src/app --include="*-calculator.tsx" | wc -l
       ```
       Expected: ~117 matches (all calculators)

    4. Verify calculator count:
       ```bash
       find src/app -name "*-calculator.tsx" -type f | wc -l
       ```
       Expected: ~117 files

    If any legacy imports found, STOP and report - migration is incomplete. Otherwise, proceed to deletion.

  </action>
  <verify>
    All grep searches return zero matches for legacy imports, ~117 matches for createCalculatorStore usage
  </verify>
  <done>
    Verification complete: migration confirmed at 100%, zero legacy hook usage in calculator components
  </done>
</task>

<task type="auto">
  <name>Delete legacy hook files</name>
  <files>
    src/hooks/use-converter.ts
    src/hooks/use-url-state.ts
  </files>
  <action>
    Delete the legacy hook files that are no longer used:

    1. Delete use-converter.ts:
       ```bash
       rm src/hooks/use-converter.ts
       ```

    2. Delete use-url-state.ts:
       ```bash
       rm src/hooks/use-url-state.ts
       ```

    3. Verify files no longer exist:
       ```bash
       ls src/hooks/use-converter.ts src/hooks/use-url-state.ts 2>&1
       ```
       Expected: "No such file or directory" errors

    Do NOT delete other hooks (use-debounce.ts, use-copy-to-clipboard.ts, etc.) - only the two legacy state management hooks.

  </action>
  <verify>
    Files src/hooks/use-converter.ts and src/hooks/use-url-state.ts do not exist (ls returns error)
  </verify>
  <done>
    Legacy hook files deleted from codebase, git working directory shows deletions
  </done>
</task>

<task type="auto">
  <name>Verify TypeScript compilation and build</name>
  <files>N/A (verification only)</files>
  <action>
    Run TypeScript compiler and build to verify no broken imports or type errors after deletion:

    1. Run TypeScript type checking:
       ```bash
       npx tsc --noEmit
       ```
       Expected: Zero errors (exit code 0)

    2. Run Biome linter:
       ```bash
       npm run lint
       ```
       Expected: Zero errors (may have warnings, but no errors)

    3. Build static export:
       ```bash
       npm run build
       ```
       Expected: Successful build, no errors

    If any errors occur, they indicate broken imports that need investigation. All three commands must pass without errors.

  </action>
  <verify>
    TypeScript compilation passes, linter passes, build completes successfully with zero errors
  </verify>
  <done>
    All automated checks pass: TypeScript types valid, lint clean, build successful - codebase functional after legacy hook removal
  </done>
</task>

</tasks>

<verification>
Run verification commands:

```bash
# Confirm files deleted
! test -f src/hooks/use-converter.ts && ! test -f src/hooks/use-url-state.ts

# Confirm no imports
! grep -r "use-converter\|use-url-state" src/app --include="*.tsx"

# Confirm types pass
npx tsc --noEmit

# Confirm build works
npm run build
```

All commands should succeed (exit code 0).
</verification>

<success_criteria>

- [ ] Zero grep matches for "use-converter" or "use-url-state" imports in src/app
- [ ] Files src/hooks/use-converter.ts and src/hooks/use-url-state.ts deleted
- [ ] TypeScript compilation passes with zero errors
- [ ] Biome lint passes with zero errors
- [ ] Production build completes successfully
- [ ] All 117 calculators using createCalculatorStore pattern
      </success_criteria>

<output>
After completion, create `.planning/phases/03-state-migration/03-01-SUMMARY.md`
</output>
